# Project Configuration

SBT project-level configuration for the Spark setup exercise is kept here. The main dependency and compiler configuration is defined in `build.sbt`.

Compile and run:

```bash
sbt clean compile
sbt "run 2"
sbt "run 4"
```
