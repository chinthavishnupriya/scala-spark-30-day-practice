# Troubleshooting

- If compilation fails, check the Scala version and collection syntax in `build.sbt`.
- If output is unexpected, verify the sample collections and customer/order IDs.
- Use `git status --short` to check whether files are ignored or untracked.
- From WSL, Windows files are normally available under `/mnt/c/`.
