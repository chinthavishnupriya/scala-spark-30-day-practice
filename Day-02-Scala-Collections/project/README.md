# Project Configuration

This directory contains SBT project-level configuration for Day 2.

Use the project configuration together with `build.sbt` to compile and run the Scala collection exercises.

```bash
sbt clean compile
sbt run
```
