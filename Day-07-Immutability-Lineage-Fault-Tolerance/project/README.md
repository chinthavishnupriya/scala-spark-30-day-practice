# Project Configuration

This directory documents the SBT project-level configuration used by Day 7.

The build uses Scala 2.13.18, Spark 4.2.0 and forked Java 17 execution with the required module-opening options.

```bash
sbt clean compile
sbt run
```
