# Project Configuration

This directory is reserved for SBT project-level configuration.

Typical SBT project files include `build.properties` and other build-tool metadata. Generated build directories and IDE metadata should remain ignored through `.gitignore`.

## Build Workflow

```bash
sbt clean compile
sbt run
```
