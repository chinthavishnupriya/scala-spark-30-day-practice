# Project Configuration

This directory documents the SBT project-level configuration for Day 9.

The project uses Scala 2.13.18, Spark 4.2.0 and local Spark execution with four cores.

```bash
sbt clean compile
sbt run
```
