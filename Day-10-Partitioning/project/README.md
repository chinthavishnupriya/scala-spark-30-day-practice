# Project Configuration

This directory documents the SBT project-level configuration for Day 10.

The application uses Scala 2.13.18, Apache Spark 4.2.0, Java 17 and local Spark execution with four cores. Java module-opening options are maintained in the project build configuration for this environment.

```bash
sbt clean compile
sbt run
```
