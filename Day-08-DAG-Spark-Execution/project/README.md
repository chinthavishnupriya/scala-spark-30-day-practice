# Project Configuration

This directory documents the SBT project-level configuration for Day 8.

The application uses Scala 2.13.18, Spark 4.2.0 and local Spark execution with four cores. Java 17 runtime options are configured for the Spark environment.

```bash
sbt clean compile
sbt run
```
